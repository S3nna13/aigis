version: "1"
aigis: guard
name: "secrets-guard"
description: "OWASP LLM06 — Secret/API key detection"

model:
  provider: openai
  model: gpt-4o-mini

guard:
  rails:
    input:
      - secrets

actions:
  secrets: block

report:
  format: json
  output_path: ./reports