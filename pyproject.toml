[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aigis"
version = "0.2.2"
description = "AI Guardrail & Integration System — unified eval, safety, and agent harness"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "aigis contributors" }]
keywords = ["ai", "guardrails", "evaluation", "llm", "safety", "harness"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
packages = [{ include = "aigis" }]

dependencies = [
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "httpx>=0.27",
    "rich>=13.0",
    "click>=8.0",
    "tomli>=2.0",
    "fastapi>=0.115",
    "uvicorn>=0.34",
]

[project.optional-dependencies]
openai = ["openai>=2.36.0"]
anthropic = ["anthropic>=0.102.0"]
ollama = ["ollama>=0.6.2"]
tokenizer = ["tiktoken>=0.12.0"]
all = ["aigis[openai,anthropic,ollama,tokenizer]"]
dev = ["pytest>=9.0.3", "pytest-asyncio>=1.3.0", "ruff>=0.15.12", "mypy>=2.1.0", "httpx"]

[project.scripts]
aigis = "aigis.cli.main:cli"

[project.urls]
Homepage = "https://github.com/S3nna13/aigis"
Documentation = "https://github.com/S3nna13/aigis#readme"
Repository = "https://github.com/S3nna13/aigis"
Issues = "https://github.com/S3nna13/aigis/issues"

[tool.hatch.build.targets.wheel]
packages = ["aigis"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = false
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests/python"]
